{% extends '::base.html.twig' %}

{% block content %}
    <h1>New Blog</h1>

    <div class="col-lg-12">
        {{ form_start(form) }}
        {{ form_widget(form) }}
        {{ form_end(form) }}
    </div>
{% endblock %}