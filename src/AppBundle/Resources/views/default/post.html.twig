{% extends '::base.html.twig' %}

{% block content %}

    <div class="col-lg-12">
        <h1>{{ blogPost.title }}</h1>
        {{ blogPost.text |raw }}
        <br />
        Publish Date: {{ blogPost.publishDate | date('Y-m-d') }}
    </div>
{% endblock %}